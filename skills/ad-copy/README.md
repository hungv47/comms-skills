# ad-copy/ — pre-staged seedbank (skill scaffold pending)

**Status:** reference-only seedbank. No `SKILL.md`, no `agents/`, no plugin-manifest entry. The future `ad-copy` skill is unbuilt — see `.agents/skill-artifacts/meta/roadmap.md` Phase 1.2 + REB-3.

**What's here:** `references/ad-intelligence/` — 3 per-surface practitioner-grade references pre-staged while source files were warm:
- `meta-retargeting.md` — warm-audience system (3 custom audiences, warm-vs-cold creative, budget pacing)
- `meta-cold-traffic.md` — subscription-app cold-traffic playbook (2-campaign structure, trial-start optimization, attribution layers)
- `creative-cadence.md` — volume / kill-speed / budget-split / dedicated-vs-repurposed creative

**Why pre-staged:** Phase 1.2 architecture is locked at Option A (separate `ad-copy` skill) per `.agents/skill-artifacts/meta/records/2026-05-08-phase-1.2-divergence-test.md`. When ad-copy scaffolds, references land here ready to consume — parallel to `short-form-brief/references/platform-intelligence/`.

**Re-entry trigger:** when `ad-copy` skill build kicks off (operator decision: ad-copy build is `≤6 months out` per T53 gate, 2026-05-10). The 3 references should be re-verified at that time — Meta's ad system shifts on quarter cadences.

**Audit note:** this directory will look "orphan" to cleanup-artifacts and similar audit tools because it has no SKILL.md. That is intentional — do not delete or relocate without checking the roadmap re-entry trigger.
